import React from "react";

const Navbar = () => {
  return (
    <div className="bg-gray-800 text-white p-4 flex justify-between items-center">
      <button className={`rounded bg-cyan-500 text-white`}> login</button>
    </div>
  );
};

export default Navbar;
